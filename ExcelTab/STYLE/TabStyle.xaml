<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ExcelTab">
    <Style x:Key="Menu" TargetType="TabItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border Name="Border" CornerRadius="0" Margin="0" Padding="8,0,10,4" BorderThickness="0" Background="Transparent" >
                        <Grid>
                            <ContentPresenter ContentSource="Header"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <!--<Setter TargetName="Border" Property="Background" Value="#555"/>-->
                            <!--<Setter TargetName="Border" Property="Background" Value="#FFF0F0F0"/>-->
                            <Setter TargetName="Border" Property="Background" Value="#FFF0F0F0"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
                            <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            <!--<Setter TargetName="TextBlock" Property="Foreground" Value="#333"/>-->
                            <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#99F0F0F0"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="transparent"/>
                            <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            <!--<Setter TargetName="TextBlock" Property="Foreground" Value="#333"/>-->
                            <!--<Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,0"/>-->
                            <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Cursor" Value="Hand"/>
                            <Setter TargetName="Border" Property="Background" Value="#FFF0F0F0"/>
                            <Setter TargetName="Border" Property="Opacity" Value="1.0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>