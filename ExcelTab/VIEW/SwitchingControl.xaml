<UserControl x:Class="ExcelTab.VIEW.SwitchingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExcelTab.VIEW"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="550" Height="300" Margin="20">
    <Border BorderThickness="2" BorderBrush="White" MouseUp="Border_MouseUp">
        <Grid Background="#BB000000">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Image x:Name="PreviewImg" MaxHeight="500" Stretch="Uniform"/>
            <Border Grid.Row="1" Background="#2ea965">
                <TextBlock x:Name="FileNameTb" Foreground="White" 
                               FontSize="15" Margin="10,5" Text="ファイル名"
                               FontWeight="Bold"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
